---
name: server-space-cleanup
description: 审计并安全回收用户服务器账号中的冗余空间，覆盖模型权重、数据集、checkpoint、缓存和虚拟环境。适用于用户要求检查、整理、去重、归档或释放服务器磁盘空间；不用于与存储空间无关的普通项目清理。
---

# 服务器冗余空间清理

帮助用户做出安全、可追溯的磁盘整理决定。默认只读；任何可能改变文件的操作前，必须先形成经过用户确认的逐项计划。

## 每次会话先确认范围

先说明本次分析的个人目录，并请用户确认或替换下列候选公共目录：

| 用途 | 候选公共目录 |
| --- | --- |
| 基础大模型、嵌入模型、重排模型及其他模型权重 | `/publicdata/model` |
| 可复用数据集 | `/publicdata/data` |

替代路径仅本次会话有效，除非用户明确要求固化为新的策略。使用前验证已确认路径存在且可读；用户未确认时，只盘点个人目录。除非用户明确纳入范围，不要把项目目录、对话项目、实验输出或任意源码目录建议迁入公共目录。

## 安全约束

- 盘点和建议阶段不得删除、移动、覆盖、重命名文件或修改权限。
- 不得检查或操作其他用户的个人目录。
- 公共目录多人可写，不得自动删除、覆盖、重命名或移动其中的内容。
- 不得仅凭文件名判断重复。应确认内容角色和版本、相对文件清单、大小及内容指纹一致。
- 对公共目录中不存在的可复用模型或数据集，先复制并校验目标，再单独询问是否删除已验证的个人源副本；不得使用未经校验的 `mv`。
- 可行时，清理近期训练输出前先检查是否存在运行中的任务。
- 每批破坏性操作都须用户点名选择展示的编号或精确路径，并再次确认具体动作；不得删除未经审查的通配路径。

## 多轮对话流程

1. **盘点：** 报告个人目录总占用、一级大目录、大文件与分类汇总；需要时使用 `scripts/inventory.py`。
2. **分类：** 标为基础模型、微调权重、checkpoint、可复用数据集、缓存、虚拟环境、压缩包/中间产物、日志或未知，并说明不确定项。
3. **对比：** 仅对基础模型和可复用数据集，在用户确认的公共目录中查重；需要判定身份时使用 `scripts/fingerprint.py`。
4. **推荐：** 用紧凑表格展示编号、类别、路径、大小、证据、风险、建议操作和预计可回收空间；将缓存、实验产物和公共归档候选项分开。
5. **确认与执行：** 迁入公共目录时先复制和校验，删除源文件前再次确认；删除个人文件时，立即重新展示精确路径。
6. **收尾：** 重新计算占用，汇总保留、复制、删除、跳过项目和实际回收空间；不确定项一律不动。

## 决策建议

- **基础模型或可复用数据集：** 若已验证公共副本等价，确认后可建议删除个人副本；若缺失，建议一个带版本号且不冲突的公共目标目录。
- **微调权重与 checkpoint：** 保留最佳 checkpoint，以及恢复有价值或正在进行实验所需的 checkpoint。文件旧只能作为审查信号，不能直接作为删除依据。
- **缓存：** 先说明是否可重新生成或下载，再建议清理。
- **虚拟环境：** 仅在用户确认不再使用且没有活跃进程依赖时建议删除。
- **未知大文件：** 只作为待审查项展示，不得自动推荐删除。

分类、公共目录对比或准备破坏性操作时，必须阅读 [references/storage-policy.md](references/storage-policy.md)。


## V2：本地路径无感切换到公共副本

当用户希望删除已验证重复的个人模型或数据集、但现有代码仍引用个人路径时，优先建议“路径交接”，而不是直接删除原路径：

1. 再次确认个人路径、公共目标、所属类别和内容哈希一致，且没有活跃任务使用该路径。
2. 先执行 `scripts/link_to_public.py <个人路径> <公共路径> --verify`，仅输出校验结果和拟执行操作。
3. 用户明确确认后，才增加 `--apply`。脚本会把个人源路径改名为同级备份，并在原路径建立指向公共副本的绝对符号链接，然后验证链接解析结果。
4. 报告保留的备份精确路径；确认代码可正常读取公共副本后，必须再次取得用户确认，才可删除该备份以回收空间。

不得对仍在写入的 checkpoint、可变数据目录、路径为个人目录根目录的对象或公共目录本身进行路径交接。若用户要求替换公共目录、处理非等价内容或链接验证失败，应停止并保留备份。
